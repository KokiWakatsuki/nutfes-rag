name: Full Reindex

on:
  workflow_dispatch:

concurrency:
  group: drive-sync
  cancel-in-progress: true  # 手動reindexを優先: 実行中のsync/reindexをキャンセルして即開始

jobs:
  reindex:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install system tools
        run: sudo apt-get install -y ghostscript libreoffice-writer libreoffice-calc libreoffice-impress

      - name: Truncate documents and full reindex
        env:
          GOOGLE_SERVICE_ACCOUNT_KEY: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_KEY }}
          GOOGLE_CLOUD_PROJECT: ${{ secrets.GOOGLE_CLOUD_PROJECT }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          GCS_BUCKET: ${{ secrets.GCS_BUCKET }}
        run: npm run reindex:ci
